# 👥 Guide Équipe - Collaborer sur RubiksCubeIA

> **🎯 Objectif :** Rejoindre le projet existant et collaborer avec l'équipe sans refaire ce qui a déjà été fait.

---

## ⚠️ IMPORTANT : Ne PAS refaire le setup initial !

Le projet a **déjà été créé** et les fichiers sont **déjà sur GitHub**.

❌ **Ce que tu ne dois PAS faire :**
- Créer un nouveau repo GitHub
- Recréer les fichiers du Rubik's Cube
- Refaire le setup Git depuis zéro

✅ **Ce que tu DOIS faire :**
- Cloner le projet existant
- Travailler sur ta partie
- Push tes modifications
- Pull les modifications des autres

---

## 📋 Table des matières

1. [Prérequis](#1️⃣-prérequis)
2. [Cloner le projet](#2️⃣-cloner-le-projet-sur-ton-pc)
3. [Importer dans Eclipse](#3️⃣-importer-dans-eclipse)
4. [Configurer Git avec TON identité](#4️⃣-configurer-ton-identité-git)
5. [Workflow de collaboration](#5️⃣-workflow-de-collaboration-quotidien)
6. [Gérer les conflits](#6️⃣-gérer-les-conflits)
7. [Bonnes pratiques](#7️⃣-bonnes-pratiques-de-léquipe)
8. [Sécurité des tokens](#8️⃣-sécurité--tokens-personnels)

---

## 1️⃣ Prérequis

### A. Installer Git

1. Télécharge Git : [https://git-scm.com/downloads](https://git-scm.com/downloads)

2. **IMPORTANT lors de l'installation** :
   - Choisis : ✅ `"Git from the command line and also from 3rd-party software"`
   - Coche : ✅ `Use bundled OpenSSH`

3. Vérifie l'installation :
   ```bash
   git --version
   ```

### B. Avoir un compte GitHub et créer un token

#### Créer un compte GitHub

1. Va sur [GitHub.com](https://github.com/signup)
2. Crée ton compte avec **TON email personnel**
3. Choisis un **username** (ce sera visible sur les commits)

#### Être ajouté comme collaborateur

⚠️ **AVANT de cloner**, tu dois être invité au projet :

1. **Demande à Blandine** de t'ajouter comme collaborateur
2. Tu recevras un **email d'invitation**
3. **Accepte l'invitation** dans l'email
4. Va sur https://github.com/Blandinekeza/RubiksCubelA
5. Tu dois maintenant voir le code ✅

#### Créer ton token personnel

**Chaque membre doit créer SON propre token** (ne jamais partager) :

1. Connecte-toi sur GitHub avec ton compte
2. Va sur : https://github.com/settings/tokens
3. **Generate new token (classic)**
4. **Note** : `Eclipse RubiksCube Projet`
5. **Expiration** : 90 days (ou plus)
6. ✅ **Coche `repo`** (toutes les permissions repo)
7. **Generate token**
8. **COPIE-LE** (commence par `ghp_...`) et **sauvegarde-le** dans un fichier

---

## 2️⃣ Cloner le projet sur ton PC

### 🅰️ Méthode A : Avec Eclipse (RECOMMANDÉ)

#### Étape 1 : Clone depuis Eclipse

1. **Ouvre Eclipse**

2. **File** → **Import...**

3. **Git** → **Projects from Git** → **Next**

4. **Clone URI** → **Next**

5. **Remplis les infos** :
   ```
   URI: https://github.com/Blandinekeza/RubiksCubeIA.git
   
   Host: github.com
   Repository path: /Blandinekeza/RubiksCubeIA.git
   
   User: TonUsernameGitHub
   Password: [TON TOKEN GITHUB - voir ci-dessous]
   
   ✅ Store in Secure Store
   ```

6. **Next** → Sélectionne branche **main** → **Next**

7. **Destination** : Choisis où sauvegarder (ex: `C:\Users\TonNom\git\RubiksCubeIA`)

8. **Next** → **Import existing Eclipse projects** → **Next**

9. ✅ Coche le projet → **Finish**

#### Étape 2 : Créer TON propre Token GitHub

⚠️ **IMPORTANT** : Ne demande JAMAIS le token de quelqu'un d'autre ! Chaque membre doit créer le sien.

**Pourquoi ?**
- ✅ Tes commits seront sous **TON nom**
- ✅ Sécurité : chacun contrôle son accès
- ✅ Si tu quittes le projet, on révoque juste ton token

**Comment créer ton token :**

1. **Connecte-toi sur GitHub** avec TON compte

2. Va sur : https://github.com/settings/tokens

3. **Generate new token (classic)**

4. **Remplis** :
   ```
   Note: Eclipse Collaboration RubiksCube
   Expiration: 90 days
   ```

5. ✅ **Coche `repo`** (toutes les sous-cases se cochent automatiquement) :
   ```
   ✅ repo
      ✅ repo:status
      ✅ repo_deployment
      ✅ public_repo
      ✅ repo:invite
      ✅ security_events
   ```

6. **Scroll en bas** → **Generate token**

7. **COPIE LE TOKEN** (commence par `ghp_...`)

8. ⚠️ **Sauvegarde-le dans un fichier texte sécurisé** (tu ne le reverras plus !)

9. **C'est CE token** que tu utiliseras pour cloner le projet

---

### 🅱️ Méthode B : En ligne de commande puis import

#### Étape 1 : Clone avec Git

1. **Ouvre le terminal** (Git Bash ou PowerShell)

2. **Va dans ton dossier de travail** :
   ```bash
   cd C:/Users/TonNom/Documents
   ```

3. **Clone le repo** :
   ```bash
   git clone https://github.com/Blandinekeza/RubiksCubeIA.git
   ```

4. **Entre tes identifiants** :
   ```
   Username: TonUsernameGitHub
   Password: [TON TOKEN]
   ```

#### Étape 2 : Importe dans Eclipse

1. **Eclipse** → **File** → **Import...**

2. **General** → **Existing Projects into Workspace** → **Next**

3. **Browse** → Sélectionne le dossier `RubiksCubeIA`

4. ✅ Coche le projet → **Finish**

---

## 3️⃣ Importer dans Eclipse

### Vérifier que tout fonctionne

1. **Dans Package Explorer**, tu devrais voir :
   ```
   📘 RubiksCubeIA [RubiksCubeIA main]
     ► src
       ► (default package)
         Main.java
         Solver.java
         ...
       ► org.kociemba.twophase
         ...
     ► JRE System Library
   ```

2. **Teste que ça compile** :
   - **Ouvre `Main.java`**
   - **Clic droit** → **Run As** → **Java Application**
   - ✅ L'interface du Rubik's Cube devrait s'ouvrir !

3. **Vérifie Git** :
   - **Clic droit sur le projet** → Tu devrais voir **Team** avec options Git

---

## 4️⃣ Configurer ton identité Git

**Avant de faire des commits**, Git doit savoir qui tu es :

```bash
git config --global user.name "Ton Prénom Nom"
git config --global user.email "ton-email@users.noreply.github.com"
```

💡 **Astuce** : Tu peux trouver ton email GitHub noreply sur [GitHub Settings > Emails](https://github.com/settings/emails). Ca c'est dans le cas où tu ne veux pas utiliser ton mail personnel sur github.

### Vérification

```bash
git config --list
```

---

## 5️⃣ Workflow de collaboration quotidien

### 📥 **TOUJOURS commencer par PULL**

Avant de travailler, **récupère les dernières modifications** de l'équipe :

```bash
git pull
```

Ou dans Eclipse :
1. **Clic droit sur le projet**
2. **Team** → **Pull**

---

### 💻 **Travaille sur ta partie**

1. **Modifie les fichiers** dont tu es responsable
2. **Teste ton code**
3. **N'oublie pas de sauvegarder** (Ctrl+S)

---

### 📤 **Push tes modifications**

#### Méthode Eclipse (SIMPLE)

1. **Clic droit sur le projet** → **Team** → **Commit...**

2. **Sélectionne les fichiers modifiés** (glisse vers "Staged Changes")

3. **Écris un message descriptif** :
   ```
   Ajout de l'algorithme A* pour le solver
   
   - Implémentation de la classe AStarAgent
   - Heuristique de distance de Manhattan
   - Tests unitaires
   ```

4. **Commit and Push**

#### Méthode Terminal

```bash
# 1. Voir ce qui a changé
git status

# 2. Ajouter tes modifications
git add .

# 3. Créer un commit
git commit -m "Ajout de l'algorithme A*"

# 4. Pousser sur GitHub
git push
```

---

### 🔄 **Le cycle complet**

```
📥 PULL (matin)
   ↓
💻 TRAVAILLE (code, teste)
   ↓
💾 COMMIT (sauvegarde locale)
   ↓
📤 PUSH (envoie sur GitHub)
   ↓
📥 PULL (avant de repartir travailler)
```

---

## 6️⃣ Gérer les conflits

### Qu'est-ce qu'un conflit ?

Un conflit arrive quand **deux personnes modifient le même fichier en même temps**.

### Comment ça se passe ?

1. Tu fais `git pull`
2. Git dit : **"CONFLICT"**
3. Les fichiers concernés ont des marqueurs :

```java
<<<<<<< HEAD
// TON code
int x = 5;
=======
// Le code de ton collègue
int x = 10;
>>>>>>> main
```

### Comment résoudre ?

1. **Ouvre le fichier** dans Eclipse

2. **Décide quelle version garder** :
   - Ton code ?
   - Le code de ton collègue ?
   - Un mix des deux ?

3. **Supprime les marqueurs** `<<<<<<<`, `=======`, `>>>>>>>`

4. **Garde le code final** :
   ```java
   int x = 10; // On garde la version du collègue
   ```

5. **Sauvegarde** (Ctrl+S)

6. **Marque le conflit comme résolu** :
   ```bash
   git add .
   git commit -m "Résolution conflit: variable x"
   git push
   ```

---

## 7️⃣ Bonnes pratiques de l'équipe

### ✅ À FAIRE

1. **TOUJOURS pull avant de commencer** à travailler
2. **Commit souvent** (petits commits réguliers)
3. **Messages clairs** : "Ajout de X" pas "update"
4. **Push à la fin de ta session** de travail
5. **Communiquer** quand tu travailles sur un fichier important
6. **Tester** ton code avant de push
7. **Travailler sur des fichiers différents** quand possible

### ❌ À ÉVITER

1. ❌ Modifier des fichiers sans pull d'abord
2. ❌ Commit des fichiers avec des erreurs de compilation
3. ❌ Messages vagues : "fix", "update", "test"
4. ❌ Travailler sur `Main.java` en même temps qu'un collègue
5. ❌ Push sans avoir testé
6. ❌ Forcer un push avec `--force` (sauf si tu sais ce que tu fais)

---

## 🗂️ Organisation du travail en équipe

### Répartition recommandée

```
📦 RubiksCubeIA(propose par IA)
├── 📁 ai/                  ← Votre travail d'IA
│   ├── AStarSolver.java    ← Personne A
│   ├── QLearning.java      ← Personne B
│   ├── DeepQLearning.java  ← Personne C
│   └── Heuristics.java     ← Personne D
│
├── 📁 training/
│   ├── Trainer.java        ← Personne A
│   └── Evaluator.java      ← Personne B
│
├── 📁 utils/
│   └── CubeEnvironment.java ← Tous
│
└── Main.java               ← NE PAS TOUCHER (déjà fonctionnel)
    Solver.java             ← NE PAS TOUCHER (déjà fonctionnel)
```

### Création de packages

Pour créer vos packages d'IA :

1. **Clic droit sur `src`** → **New** → **Package**
2. **Name** : `ai` (ou `ai.agents`, `ai.training`, etc.)
3. **Finish**
4. **Créez vos classes dedans**

---

## 📞 Communication d'équipe

### Avant de commencer à travailler

Écris dans le groupe :
> "Je travaille sur `AStarSolver.java` ce matin"

### Avant de push

Si gros changement :
> "Je vais push l'algo A* complet, pensez à pull !"

### Si conflit

> "J'ai un conflit sur `CubeEnvironment.java`, on discute ?"

---

## 8️⃣ Sécurité & Tokens personnels

### 🔐 Règles d'or des tokens

#### ✅ À FAIRE

1. **Créer TON propre token** (jamais utiliser celui de quelqu'un d'autre)
2. **Ne JAMAIS partager ton token** (même pas avec l'équipe)
3. **Sauvegarder ton token** dans un fichier sécurisé local
4. **Utiliser un token différent** pour chaque projet/machine
5. **Révoquer ton token** si tu ne travailles plus sur le projet

#### ❌ À NE JAMAIS FAIRE

1. ❌ Partager ton token sur Discord/WhatsApp/Email
2. ❌ Commit ton token dans le code
3. ❌ Écrire ton token dans le README
4. ❌ Utiliser le token de Blandine ou d'un coéquipier
5. ❌ Laisser ton token dans un fichier partagé (Google Drive, etc.)

### 🆘 Si tu perds ton token

**Pas de panique !** Tu peux en créer un nouveau :

1. Va sur https://github.com/settings/tokens
2. **Generate new token (classic)**
3. Donne-lui les mêmes permissions (`repo`)
4. Utilise ce nouveau token dans Eclipse

### 🗑️ Si tu quittes le projet

**Pense à révoquer ton token** :

1. Va sur https://github.com/settings/tokens
2. Trouve ton token `Eclipse RubiksCube Projet`
3. **Delete** ou **Revoke**

---

## 🆘 Problèmes courants

### ❌ "Permission denied"

**Solution :** Tu n'es pas collaborateur du repo.
- Demande à Blandine de t'ajouter sur GitHub

### ❌ "Your local changes would be overwritten"

**Solution :**
```bash
# Sauvegarde tes modifs
git stash

# Pull les changements
git pull

# Récupère tes modifs
git stash pop
```

### ❌ "fatal: refusing to merge unrelated histories"

**Solution :**
```bash
git pull origin main --allow-unrelated-histories
```

---

## 📊 Commandes Git essentielles

| Commande | Description |
|----------|-------------|
| `git pull` | Récupérer les dernières modifications |
| `git status` | Voir ce qui a changé |
| `git add .` | Préparer tous les fichiers |
| `git commit -m "message"` | Créer un commit |
| `git push` | Envoyer sur GitHub |
| `git log` | Voir l'historique |
| `git diff` | Voir les modifications détaillées |

---

## 🎯 Checklist du collaborateur parfait

- [ ] J'ai cloné le projet
- [ ] J'ai configuré mon identité Git
- [ ] Je peux lancer `Main.java` et voir l'interface
- [ ] Je sais faire `git pull`
- [ ] Je sais faire `git commit`
- [ ] Je sais faire `git push`
- [ ] J'ai créé mon package `ai`
- [ ] Je communique avec l'équipe
- [ ] Je pull avant de travailler
- [ ] Je teste avant de push

---

## 🎓 Ressources

- [Documentation Git](https://git-scm.com/doc)
- [GitHub Flow](https://docs.github.com/en/get-started/quickstart/github-flow)
- [Résoudre les conflits](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/addressing-merge-conflicts/resolving-a-merge-conflict-using-the-command-line)

---

## 👥 Contacts équipe

- Blandine KEZA - [@Blandinekeza](https://github.com/Blandinekeza)
- **Repo GitHub** : https://github.com/Blandinekeza/RubiksCubeIA
---
